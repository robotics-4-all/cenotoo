{{- if .Values.monitoring.enabled }}
{{- if .Values.monitoring.kafka.metricsEnabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ include "cenotoo.fullname" . }}-kafka
  labels:
    {{- include "cenotoo.labels" . | nindent 4 }}
    strimzi.io/cluster: {{ include "cenotoo.fullname" . }}-kafka
spec:
  selector:
    matchLabels:
      strimzi.io/cluster: {{ include "cenotoo.fullname" . }}-kafka
      strimzi.io/kind: Kafka
  podMetricsEndpoints:
    - path: /metrics
      port: tcp-prometheus
      interval: {{ .Values.monitoring.scrapeInterval }}
      relabelings:
        - sourceLabels: [__meta_kubernetes_pod_label_strimzi_io_name]
          targetLabel: kafka_resource
{{- end }}
---
{{- if .Values.monitoring.kafka.kafkaExporterEnabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ include "cenotoo.fullname" . }}-kafka-exporter
  labels:
    {{- include "cenotoo.labels" . | nindent 4 }}
    strimzi.io/cluster: {{ include "cenotoo.fullname" . }}-kafka
spec:
  selector:
    matchLabels:
      strimzi.io/cluster: {{ include "cenotoo.fullname" . }}-kafka
      strimzi.io/kind: KafkaExporter  
  podMetricsEndpoints:
    - path: /metrics
      port: tcp-prometheus
      interval: {{ .Values.monitoring.scrapeInterval }}
{{- end }}
---
{{- if .Values.monitoring.flink.metricsEnabled }}
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: {{ include "cenotoo.fullname" . }}-flink
  labels:
    {{- include "cenotoo.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: {{ include "cenotoo.fullname" . }}-flink
      type: flink-native-kubernetes
  podMetricsEndpoints:
    - port: prom
      interval: {{ .Values.monitoring.scrapeInterval }}
{{- end }}
{{- end }}
