apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cenotoo.fullname" . }}-kafka-credentials
  labels:
    {{- include "cenotoo.labels" . | nindent 4 }}
type: Opaque
data:
  # Base64-encoded credentials — override in production via sealed-secrets or external-secrets
  username: {{ "cenotoo" | b64enc | quote }}
  password: {{ "changeme" | b64enc | quote }}
